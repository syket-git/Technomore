import React, { FC } from 'react';
import Layout from '../../layouts';

const Dashboard: FC = () => {
  return (
    <Layout>
      <div>Dashboard</div>
    </Layout>
  );
};

export default Dashboard;
